# Encryption key (32 bytes hex): openssl rand -hex 32
SECRET_KEY=

# PostgreSQL connection string (shared with inventory-ops)
DATABASE_URL=postgresql://inventory:inventory@localhost:5433/inventory_ops

# Installation domain restriction (regex pattern)
ALLOWED_DOMAIN_PATTERN=/.*/

# Optional: APL storage (default: file for local dev)
# APL=file

# Optional: Log level
# APP_LOG_LEVEL=info

# Optional: Override iframe/API URLs (for local dev)
APP_IFRAME_BASE_URL=http://localhost:3004
APP_API_BASE_URL=http://localhost:3004

# Default currency for transactions
DEFAULT_CURRENCY=USD

# Stripe Terminal (Phase 2)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_TERMINAL_LOCATION_ID=tml_...
